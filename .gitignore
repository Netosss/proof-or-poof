# Virtual Environment
venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Local analysis/diagnostic tooling — generated outputs and ad-hoc scripts.
# Operational scripts that the operator/CI needs are explicitly whitelisted below.
# (dir-form `scripts/` would override the `!`-prefixed whitelist entries; the
# `dir/*` form allows per-file exceptions.)
scripts/*
!scripts/enterprise_admin.py
!scripts/sign_request.py
!scripts/redis_clear_forensic_cache.py
results.jsonl
dry_run_results.jsonl
*.log
gold_config_report.txt
analysis_run.log
stress_test.log

# macOS
.DS_Store

# Tooling and Temporary Files
agent-tools/
.cursor/
node_modules/

# Models (Local only, baked into Docker)
models/big-lama.pt

# Local AI Prompt Testing
test_prompt_accuracy.py
test_prompt_gold.json.env

.env
