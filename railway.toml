[build]
builder = "dockerfile"

[deploy]
# Railway will provide the $PORT automatically
startCommand = "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"
healthcheckPath = "/health"
healthcheckTimeout = 30   # in seconds
restartPolicyType = "ON_FAILURE"
